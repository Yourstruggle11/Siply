{
  "name": "siply-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "mv ../tsconfig.json ../tsconfig.mobile.json || true; astro check && astro build; mv ../tsconfig.mobile.json ../tsconfig.json || true",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@tailwindcss/vite": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
